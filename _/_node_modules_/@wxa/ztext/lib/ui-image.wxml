<view class="block-class">
    <image lazyLoad bind:tap="handleTap" class="image {{class}}" data-src="{{src}}" mode="widthFix" src="{{src}}" style="width: {{width}}px; height: {{height}}px;"></image>
    <rich-text nodes="{{restNodes}}"></rich-text>
</view>
