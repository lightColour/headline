module.exports = (function() {
    arguments.length = arguments.length;
    var classes = [];
    var length = arguments.length;
    for (var i = 0; i < length; i++) {
        var arg = arguments[((nt_0 = (i), null == nt_0 ? undefined : 'Number' === nt_0.constructor ? nt_0 : "" + nt_0))];
        var nextArg = arguments[((nt_1 = (i + 1), null == nt_1 ? undefined : 'Number' === nt_1.constructor ? nt_1 : "" + nt_1))];
        if (typeof arg !== 'string' || arg === '') continue;;
        if (i === length - 1) classes.push(arg);;
        if (!!nextArg || nextArg === '') {
            classes.push(arg)
        }
    };
    return (classes.join(' '))
});